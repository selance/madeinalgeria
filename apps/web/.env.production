# Baked into prod builds (CI overrides via env vars when needed).
PUBLIC_API_BASE_URL=https://api.madeinalgeria.dev
PUBLIC_APP_BASE_URL=https://www.madeinalgeria.dev
# GA4 wired directly (no GTM tag to publish). Production only.
# NOTE: GTM is still loaded below — do NOT also add a GA4 tag inside GTM or
# pageviews will be counted twice.
PUBLIC_GA4_ID=G-YFL51V1TBS
PUBLIC_GTM_ID=GTM-WSSKJJP6
