# Loaded by `astro build --mode staging` (deploy:staging) — keeps staging
# builds from inheriting .env.production's prod URLs.
PUBLIC_API_BASE_URL=https://mia-api-staging.<CF_SUBDOMAIN>.workers.dev
PUBLIC_APP_BASE_URL=https://mia-app-staging.<CF_SUBDOMAIN>.workers.dev
